[pytest]
# 测试目录
testpaths = tests

# 只收集 pytest 标准文件，忽略脚本风格测试
python_files = test_unit.py
python_classes = Test*
python_functions = test_*

# 输出格式
addopts = -v --tb=short

# 忽略非测试文件
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
